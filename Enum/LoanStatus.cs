namespace FourtitudeIntegrated.Enum
{
    public enum LoanStatus
    {
        Unpaid,
        Overdue,
        Partial,
        Cleared
    }

    public enum LoanTransactionType
    {
        Disbursement,
        Payment
    }
}
