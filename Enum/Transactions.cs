namespace FourtitudeIntegrated.Enum
{
    public enum EntryType
    {
        Debit,
        Credit
    }
    public enum TransactionType
    {
        Deposit,
        Withdrawal,
        Transfer
    }
}
